package-manager-strict-version=false

# Supply-chain hardening
# Block ALL install/postinstall scripts by default.
# Packages that need native builds are allowlisted in package.json
# under pnpm.onlyBuiltDependencies.
ignore-scripts=true
