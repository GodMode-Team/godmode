/**
 * proof-viewer.ts — STUB (removed in v2 slim, sidebar editor replaces later)
 */
import { html, type TemplateResult } from "lit";

export function renderProofViewer(_props: any): TemplateResult {
  return html``;
}
