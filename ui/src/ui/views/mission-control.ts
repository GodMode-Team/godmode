/**
 * mission-control.ts — STUB (removed in v2 slim, replaced by Paperclip)
 */
import { html, type TemplateResult } from "lit";

export function renderMissionControl(_props: any): TemplateResult {
  return html`<div class="view-panel" style="padding: 24px;">
    <h2>Mission Control</h2>
    <p style="color: var(--text-secondary, #888);">Agent orchestration moved to Paperclip. Coming soon.</p>
  </div>`;
}
